import Link from "next/link";
import { HeaderContainer, HeaderContent } from "./styles";

export function Header() {
  return (
    <HeaderContainer>
      <HeaderContent>
        <h1>Pull Snap</h1>
      </HeaderContent>
    </HeaderContainer>
  );
}
